---
title: 창조 터보
description: 처음부터 새로운 Turborepo 저장소를 빠르게 설정하세요.
product: turborepo
type: reference
summary: 새로운 Turborepo 모노레포를 스캐폴드하는 `create-turbo` CLI에 대한 참조입니다.
related:
- /docs/getting-started/installation
---

Turborepo를 시작하는 가장 쉬운 방법은 `create-turbo`을 사용하는 것입니다. 이 CLI 도구를 사용하면 모든 것이 설정된 새로운 모노레포 구축을 빠르게 시작할 수 있습니다.

<PackageManagerTabs>

<Tab value="pnpm">
```bash title="Terminal"
pnpm dlx create-turbo@latest
```

</Tab>

<Tab value="yarn">
```bash title="Terminal"
yarn dlx create-turbo@latest
```

</Tab>

<Tab value="npm">
```bash title="Terminal"
npx create-turbo@latest
```

</Tab>

<Tab value="bun">
```bash title="Terminal"
bunx create-turbo@latest
```

</Tab>

</PackageManagerTabs>

## 예제로 시작

커뮤니티는 Turborepo에서 일반적인 도구와 라이브러리를 사용하는 방법을 보여주기 위해 일련의 예제를 선별합니다. 예제 중 하나로 모노레포를 부트스트랩하려면 `--example` 플래그를 사용하세요.

<PackageManagerTabs>

<Tab value="pnpm">
```bash title="Terminal"
pnpm dlx create-turbo@latest --example [example-name]
```

</Tab>

<Tab value="yarn">
```bash title="Terminal"
yarn dlx create-turbo@latest --example [example-name]
```

</Tab>

<Tab value="npm">
```bash title="Terminal"
npx create-turbo@latest --example [example-name]
```

</Tab>

<Tab value="bun">
```bash title="Terminal"
bunx create-turbo@latest --example [example-name]
```

</Tab>

</PackageManagerTabs>

아래 예시 이름 중 하나를 사용하세요.

## 핵심 관리 예시

다음 예는 Turborepo 핵심 팀에서 유지 관리합니다. 종속성은 가능한 한 최신 상태로 유지되며 이러한 예제에 대한 GitHub 문제가 허용되고 해결됩니다.

<ExamplesTable coreMaintained />

## 커뮤니티에서 관리하는 예시

커뮤니티는 Turborepo에서 일반적인 도구와 라이브러리를 사용하는 방법을 보여주기 위해 일련의 예제를 선별합니다. 예제 중 하나로 모노레포를 부트스트랩하려면 `--example` 플래그를 사용하세요.

<ExamplesTable />

### 커뮤니티 예시 사용

GitHub URL을 사용하여 사용자 정의 시작 또는 예제를 사용할 수도 있습니다. 이는 커뮤니티의 사용자 정의 스타터나 예제를 사용하는 데 유용합니다.

<PackageManagerTabs>

<Tab value="pnpm">
```bash title="Terminal"
pnpm dlx create-turbo@latest --example [github-url]
```

</Tab>

<Tab value="yarn">
```bash title="Terminal"
yarn dlx create-turbo@latest --example [github-url]
```

</Tab>

<Tab value="npm">
```bash title="Terminal"
npx create-turbo@latest --example [github-url]
```

</Tab>

<Tab value="bun">
```bash title="Terminal"
bunx create-turbo@latest --example [github-url]
```

</Tab>

</PackageManagerTabs>

## 옵션

```txt title="Terminal"
-m, --package-manager to use (choices: "npm", "yarn", "pnpm", "bun")

--skip-install: Do not run a package manager install after creating the project (Default: false)

--skip-transforms: Do not run any code transformation after creating the project (Default: false)

--turbo-version <version>: Use a specific version of turbo (default: latest)

-e, --example [name]|[github-url]: An example to bootstrap the app with. You can use an example name from the official Turborepo repo or a GitHub URL. The URL can use any branch and/or subdirectory

-p, --example-path <path-to-example>: In a rare case, your GitHub URL might contain a branch name with a slash (e.g. bug/fix-1) and the path to the example (e.g. foo/bar). In this case, you must specify the path to the example separately: --example-path foo/bar

-v, --version:  Output the current version

-h, --help: Display help for command
```
